export type GridRow = {
    label: string,
    partNumber: any,
    lastServiceDate: string,
    serviceCost: number,
    partCost: number,
    model: number,
    conceptModel: string,
    reviewedBy: string,
}